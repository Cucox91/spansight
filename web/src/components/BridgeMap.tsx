// TODO(raziel): Week 4 [ME] — first React + MapLibre GL component (map + filter state),
// translating design/mockup.html. First-of-a-kind, hand-written per docs/AI-USAGE.md;
// maplibre-gl is intentionally not a dependency yet.
function BridgeMap() {
  return (
    <section className="bridge-map-placeholder" aria-label="Bridge map placeholder">
      <p>Map explorer lands in Week 4.</p>
    </section>
  )
}

export default BridgeMap

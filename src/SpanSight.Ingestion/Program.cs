// SpanSight.Ingestion — CLI worker: download → parse → validate → load → tiles (IMPLEMENTATION-PLAN §2).
//
// TODO(raziel): Week 2 [ME] — NBI parser core + DMS→decimal converter land in SpanSight.Core, hand-written.
// TODO(raziel): Week 2 [AI, after the [ME] core] — CLI plumbing: config, logging, idempotent re-run wrapper (FR-0.1).

Console.WriteLine("SpanSight.Ingestion scaffold — pipeline lands in Week 2.");

public partial class Program;

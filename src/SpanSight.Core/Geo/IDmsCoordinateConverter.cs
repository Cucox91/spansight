namespace SpanSight.Core.Geo;

/// <summary>
/// Converts NBI DMS-encoded coordinates (items 16/17) to decimal degrees.
/// </summary>
public interface IDmsCoordinateConverter
{
    // TODO(raziel): Week 2 [ME] — define the conversion contract (input encoding, invalid-value
    // handling for FR-0.2) and hand-write the implementation. First-of-a-kind per docs/AI-USAGE.md.
}

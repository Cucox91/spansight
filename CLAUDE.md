# SpanSight — AI Session Context

Portfolio project by Raziel Arias: a national bridge-inventory intelligence platform built on public FHWA NBI data (~624k bridges). Purpose: demonstrate senior-level C#/.NET + React + Azure engineering. This file is committed intentionally as a transparency artifact (see docs/AI-USAGE.md).

## Read before working

- `docs/SDLC.md` — lifecycle model, phase gates, change control, artifact index
- `docs/REQUIREMENTS.md` — SRS: scope, ground rules (GR-1…GR-7), use cases, FRs with acceptance criteria, NFRs
- `docs/TRACEABILITY.md` — RTM; update the row when a PR closes or changes a requirement
- `docs/ARCHITECTURE.md` — ADRs; all-Azure topology (ADR-006-B)
- `docs/IMPLEMENTATION-PLAN.md` — current WBS ([ME]/[AI] tags are historical; AI executes all tasks since AI-USAGE v1.2)
- `docs/AI-USAGE.md` — the AI policy governing every session

## Hard rules — never violate

1. **Zero FDOT content** (GR-1/GR-2): no FDOT data, APIs, branding, or references anywhere. Florida data only from federal or county sources (e.g., Miami-Dade Transit).
2. **Full AI implementation, post-completion study** (AI-USAGE v1.2, amended 2026-07-17): AI implements everything — the [ME]/[AI] task split is removed. Merges are gated by green CI + AI self-review; Raziel studies the finished codebase to mastery after the build completes. PRs with meaningful AI code get the `ai-assisted` label. **Never AI's:** credentials, API keys, billing, account actions — secrets never in the repo or a prompt. *(v1.1 required pre-merge line review — superseded; the change log records the shift.)*
3. **Small, coherent diffs.** Prefer several small conventional commits/PRs over one large one — the PR history is the study trail. CI must be green before merge; PRs with meaningful AI-written code get the `ai-assisted` label.
4. **No bulk NBI data in git** — test fixtures only (a few hundred rows max).
5. **Azure discipline:** never create/modify cloud resources except through `infra/` Bicep; monthly budget ≤ $50 (alert at $40).
6. **Not engineering advice:** UI must keep the disclaimer footer; app displays published data, never computes engineering judgments.

## Stack (pinned 2026-07)

.NET 10 LTS · ASP.NET Core + SignalR · EF Core + Npgsql + NetTopologySuite · React + TypeScript + Vite · MapLibre GL JS · PostgreSQL 16 + PostGIS · Redis (Streams) · Parquet + DuckDB (analytics, local) · tippecanoe → PMTiles · OpenTelemetry → App Insights (cloud) / Grafana (local) · Bicep · GitHub Actions (OIDC to Azure) · Node 24 LTS (GTFS-RT poller only, Phase 2).

## Conventions

Trunk-based; short-lived branches; conventional commits; PR for everything (template in `.github/`); CI must be green; `main` always deployable; xUnit + Testcontainers + one Playwright smoke; ADR in `docs/` for any surprising decision; requirement changes follow SDLC.md §4 (SRS edit + change-log row + RTM update, by PR).

## Layout

`src/SpanSight.Core|Api|Ingestion` + `src/tests/` · `web/` (SPA) · `infra/` (Bicep) · `tools/` (tiles/DuckDB scripts) · `docs/` (SDLC set).

## Current status (keep this section updated at every phase gate)

**Phase 0 core merged to `main` 2026-07-17 (PRs #2, #3); real 2025 snapshot loaded; policy now AI-USAGE v1.2 (full AI build, Raziel studies post-completion).** The Weeks 2–4 pass was authored in Cowork under v1.1, verified and committed by Claude Code, line-reviewed and merged by Raziel the same day — the last pre-merge line review before v1.2 took effect. Also that day: SDLC formalized (`docs/SDLC.md`, SRS v1.1 with Phase 0 acceptance criteria + Phase 0.5 AI series FR-AI.1–3, `docs/TRACEABILITY.md` RTM, ADR-008) · `tools/build-tiles.sh` + root README + `SpanSight.Api.http` refresh (PR #3).

Delivered: Core domain (parser, DMS converter, validators, classifier, lookups, EF model + `InitialSchema` migration, BridgeFilter, AI port scaffold) · Ingestion CLI (idempotent unnest-upsert load, quarantine, run summaries, GeoJSON export) · API (bridges/geojson/detail/lookups/stats/qa/ai endpoints, ProblemDetails, rate limiting, CORS, health, OpenAPI+Scalar, OTel config-gated) · test suites (unit + Testcontainers integration) · fixture + generator · web SPA (explorer, drawer, QA page per mockup).

**Verified on the dev Mac 2026-07-17:** `dotnet build` 0 warnings · `dotnet format` clean · **106/106 tests green** including the Testcontainers integration suite against real PostGIS · `web/` lint + build clean · full-stack smoke pass (fixture load 114/99/15 + idempotent no-op rerun, map → filter → drawer deep link `/bridge/FL/…` → `/qa` reconciling exactly). Four small fixes landed during verification: GeoJSON typing in `BridgeMap`, root-anchored `/data/` gitignore (bare `data/` swallowed `src/**/Data/` on case-insensitive macOS), ingestion CLI content root, AbortError filter in `QaPage`.

Earlier done: docs set v1, repo public, Week 1 scaffold on `main`, Azure trial ($200/30d — upgrade to PAYG before it lapses). **Swiftly access granted 2026-07-17** (agencyKey `miami`, GTFS-rt trip updates + vehicle positions, 180 req/15 min) — risk R-4 fallback closed; key lives in Raziel's password manager, enters `.env`/GitHub secrets only when Phase 2 starts (NFR-8 §10 — never in the repo).

**Real 2025 snapshot loaded locally 2026-07-17:** 743,398 read · 741,131 loaded · 2,267 quarantined (0.30%) from `data/2025AllRecordsDelimitedAllStates.txt` (291 MB, gitignored).

**Done 2026-07-18 (post-v1.2):** EXPLAIN pass at 741k rows (0.9–31 ms hot shapes; NFR-1 evidence in RTM) · national tiles (741,131 → 22.4 MB PMTiles, run-linked manifest) · Playwright smoke + axe scan in CI, all green (PR #5; one real a11y finding fixed) · FR-0.1–0.4 Done in RTM · deploy stack authored: budget + container-app Bicep modules, API Dockerfile, Entra-token DB auth (`Database:UseEntraToken`), Azure Monitor OTel gate, `deploy.yml` (OIDC → Bicep → migration → GHCR → SWA → smoke), `docs/RUNBOOK.md`.

**DEMO LIVE 2026-07-19:** SPA `https://kind-river-0d5c2f510.7.azurestaticapps.net` · API `https://ca-spansight-api-demo.wonderfulforest-bd8cc0ce.southcentralus.azurecontainerapps.io`. Nine Deploy runs to first green (each failure one layer deeper — RUNBOOK §7 is the study trail; PRs #10–#12 infra fixes). Then: 741,131 bridges loaded to cloud PG (run #2, reconciling exactly with local), 22.4 MB PMTiles + manifest in Blob, `VITE_TILES_URL` set, storage CORS by Bicep (PR #13), deploy-on-main trigger (PR #14), two tiles-mode-only SPA bugs found live and fixed (PR #15 stuck skeleton, PR #16 condition colors — CI e2e runs fallback mode and can't catch either), final dispatch with `run_e2e: true` **green: full Playwright + axe suite against the live demo**. One human step beyond RUNBOOK §1: `GRANT azure_pg_admin TO "spansight-deploy"` (postgis is a gated extension; permission classifier correctly kept the role escalation with Raziel).

**Custom domain 2026-07-24:** `www.spansights.com` + apex `spansights.com` (GoDaddy DNS) — `spaCustomDomains`/`spaApexDomain` in `infra/main.bicepparam` bind both SWA hostnames (www = cname-delegation; apex = dns-txt-token + A record to `stableInboundIP` — GoDaddy has no ALIAS) and widen both CORS allowlists (API `Cors__Origins__N` indexed env, storage PMTiles rules) to arrays. Ordering + the mid-deploy TXT token dance + verification: RUNBOOK §8 ([RAZIEL] all GoDaddy records; no domain forwarding — it conflicts with the apex A record). Canonical URL `https://www.spansights.com` (apex is single-region by design). README front door now links the live demo. Post-cutover deploy re-run went green; API + storage CORS verified live from both new origins 2026-07-24.

**PHASE 0 GATE PASSED 2026-07-24** (IMPLEMENTATION-PLAN §9): FR-0.1–0.6 Done in RTM v1.1 · two waivers with P1-W1 close-out tasks (container-scan CI job; coverage report + ≥70% gate — neither was in CI) · TEST-PLAN.md exception → P1-W1 · SRS v1.3 elaborates the Phase 1 slice (FR-1.1–1.5 ACs, GR-6 descriptive-statistics guardrail on FR-1.3) · Phase 1 WBS is IMPLEMENTATION-PLAN §10 with **lane tags: [CC] Claude Code implementation · [CW] Cowork docs · [RAZIEL] human-only** · README "How AI was used" now matches AI-USAGE v1.2 · [RAZIEL] sign-off items open in the gate note (spend figure, App Insights trace spot-check, secret-push-protection confirm). Phase 1 runs on the Mac; Windows migration decision at the P1→P2 seam.

**PHASE 1 DATA FOUNDATION DONE 2026-07-26 (P1-W2).** **FR-1.1 → Done:** all 34 NBI vintages 1992–2025 converted and reconciled — **22,307,363 source rows = 22,307,362 converted + 1 rejected** (0.000004%), 1.4 GB of Parquet, every vintage `reconciles: true` in `tools/vintages/catalog.json`. The full run found what the three-year sample could not: FHWA published **five** column layouts, not three (2016 adds `CAT10`; 2017–18 add `CAT23`/`CAT29` — proven to be the same quantities 2019+ calls `BRIDGE_CONDITION`/`LOWEST_RATING`/`DECK_AREA`, carried faithfully and coalesced only in `catalog.sql`), and the era sequence is **not monotonic** (10-yr-rule columns present 1992–2009, absent 2010–11, present again 2012–18), so all 34 year→era mappings are now pinned from real headers. The one reject is 2024 line 459,987 — an embedded comma in `OTHR_STATE_STRUC_NO_099` (`1sDPG, 2sDIB`) that would otherwise have stored `BRIDGE_CONDITION = '42'`. `tools/vintages/catalog.sql` is the AC-4 entry point (proved by a fresh-clone test); `archive-to-blob.sh` + RUNBOOK §10 cover AC-3 (the `parquet-archive` container + cool-tier lifecycle already existed in Bicep from Phase 0 — no infra diff needed). **SRS v1.4** amends FR-1.1 AC-3's "≤3 vintages" fixture cap to one per published layout (SDLC §4 change control; read literally it would have left two real layouts untested). **FR-1.5 → In progress:** TIGER 2025 + ACS 5-year 2024 staging merged — `tools/census/`, boundaries usable for point-in-polygon, population keyed by 5-digit text FIPS, provenance in `catalog.json`; the key-free bulk path is used because api.census.gov now requires a key for every data query. Join + coverage metric are W5. Two discontinuities documented for GR-6: the population moves at 2009→2010 (713,115 → 604,493), and Connecticut's 9 planning regions break joins to pre-2022 county codes.

**PHASE 1 CONDITION TRENDS DONE 2026-07-26 (P1-W3). FR-1.2 → Done.** Offline DuckDB job (`tools/trends/`) → **1,039,109 structures · 20,649,259 observations · 111,335 county/state rollup rows**, reconciling exactly with the vintage catalog (22,307,362 converted − 1,655,245 non-type-1 − 2,858 duplicate-key). Serving side: new `analytics` schema (EF migration `AnalyticsConditionTrends`), `load-trends` CLI (idempotent, provenance-stamped with job run id + catalog SHA, converges by deleting rows the new job no longer contains), `/api/bridges/{state}/{sn}/history` + `/api/trends`, drawer condition sparkline, deep-linkable `/trends` view.

The run answered the **2009→2010 population discontinuity** noted above: it is not bridges disappearing. FHWA stopped exporting non-type-1 records (routes *under* a structure) after 2009 — raw counts fall 713,115 → 604,493 (−15.2%), but type-1 only is 608,419 → 604,493 (−0.6%). Filtering to record type `1` is also right on the merits: **0 of 18,036** non-type-1 rows in 1992 carry any numeric condition rating, so they can only add double-counts and phantom "Unrated" years. Rule documented in `tools/trends/trends.sql` and its README.

Anti-drift is the load-bearing test: the G/F/P rule now exists in C# (`ConditionClassifier`) *and* SQL, so `ConditionTrendGoldenTests` executes `trends.sql` itself over the era fixtures and compares both lowest-rating and class against the classifier on all 1,500 rows — mutation-verified (`>= 7` → `>= 8` in the SQL fails it). Four bridges pinned by hand. **Storage measured, not assumed:** packed one-row-per-bridge 228 MB / 1.04M rows vs skinny 1,737 MB / 20.6M rows (7.6×) — the B1ms justification; cost is that the packed column can't be aggregated in SQL, which is why the rollups are relational. EXPLAIN at full scale: index scans, 0.04–0.08 ms; API p95 **2.0–2.6 ms** vs NFR-1's 300 ms. Publish procedure is RUNBOOK §10.3 ([RAZIEL] — additive, `core` untouched, `TRUNCATE analytics.trend_run CASCADE` rolls back).

**PHASE 1 DETERIORATION PATTERNS DONE 2026-07-26 (P1-W4). FR-1.3 → Done.** Offline DuckDB job (`tools/deterioration/`) → **33 year-pairs 1992–2025 · 19,537,768 structure pairs · 49,988,580 component pairs · 303 cohorts · 6,313 matrix rows · 27,842 non-zero cells**, on the same population FR-1.2 uses (record type 1, SOURCE_ROW tie-break) so the two jobs reconcile by construction. Cohort = structure type group (43B) × material group (43A) × NOAA nine-region climate map, the region map a static lookup. Serving side: `analytics.deterioration_{run,matrix_row,matrix_cell}` (EF migration `AnalyticsDeteriorationMatrices`), idempotent `load-deterioration` CLI with the same convergence-delete as FR-1.2, `/api/deterioration/{cohorts,matrix}`, and a routed `/patterns` view.

The guardrail work *is* the feature here (GR-6). The sample-size floor is **n ≥ 50 per matrix row** — the row, because the row total is every rate's denominator — and it is defined once in the SQL, carried in the manifest, stamped on the run and applied by the API, which returns `sufficient:false` with **every rate null** rather than a number a view could render. Counts and the row's year-span are always served: suppression removes the rate, never the evidence. At full scale that suppresses 3,227 of 6,273 cohort rows (51.4%) holding 0.075% of the pairs. The API also ships the method note, methodology version, link and cadence caption with every matrix, so no view can render one bare, and **no route or payload under `/api/deterioration` identifies a structure** — asserted, not assumed.

**The methodology doc merged in the same PR at v1.1** (W4 exit: "merged with the feature, not after it"), carrying eight corrections found by measuring the full 34-vintage set before writing code — the duplicate-key rule (v1.0's "excluded entirely" contradicted its own reconciliation guarantee), the rating domain, cross-family exclusivity (30,485 structure-years publish both a culvert and a 58/59/60 rating), the "national matrix is always above floor" claim (true of this build, false at fixture scale), material groups being new rather than reused, the 10-year-rule boundary, the Connecticut claim, and the job entry point — plus six decisions v1.0 left open (cohort attributes from year *y*; a labelled "Not published" bucket; pooling all 33 year-pairs with each row's span displayed; the non-CONUS bucket as an else branch; families never summed). Each has a change-log row per SDLC §4. No SRS edit was needed.

Anti-drift again load-bearing: the cohort rule now exists in SQL, in `NbiCohorts` and in the SPA's filter rail, so the golden suite executes the job's own lookups against the C# on all 10 item-43A codes, all 23 item-43B codes and all 56 state codes in both directions, and a new test pins C# against TypeScript — parity nothing had asserted before. Mutation-verified four ways (region moved, design code moved, tie-break reversed, cohort keyed on y+1). AC-3's hand-computed matrix is a committed synthetic vintage CSV whose 30 cohort cells are each asserted; the Alabama-only era fixtures structurally cannot reach a second region, the "Other" material group, the record-type filter or the tie-break, so both fixtures exist and check each other.

Three real bugs were found by the new tests rather than in review: padding `""` to `"00"` filed "not published" under the published "Other" type group; API provenance followed the newest *completed* run instead of the run whose rows are being served (after a convergence sweep those differ); and axe caught muted text failing AA over tinted matrix cells plus a global `.count` rule overriding cell colour. A pre-merge adversarial review of the branch then found five more defects and eight wrong figures, all fixed in the same PR. The important one was another GR-6 bypass: the cadence caption published a percentage with no floor applied, so a matrix whose every row read "insufficient data" still stated a share — 395 of 1,028 real cohort matrices, 199 of them exactly 100.0%, and where such a matrix has one populated row that number *is* the rate the response had just nulled out. Also: the loader wrote before reconciling and outside a transaction; a retry after a failed load crashed on the unique job-id index; `component=99` returned 200 with a fabricated label; `det_check_span` was a tautology that could not fail; and DuckDB's `lpad` truncates, so an over-long item 43B would have joined the wrong cohort silently. EXPLAIN at full scale: 0.045–0.102 ms index scans, API p95 **4.9–8.2 ms** vs NFR-1's 300 ms; 6.9 MB served. Suite **430 green** (Core 249 · Api 148 · Ingestion 33) + 20 Playwright/axe, coverage 88.5% scoped. Publish procedure is RUNBOOK §10.4 ([RAZIEL] — additive, independent of FR-1.2, `TRUNCATE analytics.deterioration_run CASCADE` rolls back).

Known follow-up outside this PR: `tools/trends/trends.sql` and `tools/census/README.md` both state that NBI adopted Connecticut's 2022 planning regions. It did not — item 3 publishes the eight legacy codes in every vintage through 2025 — and the correction is queued as its own docs-only change to keep this diff coherent.

Next: **Phase 0.5 flip (parallel with P1-W1, does not gate Phase 1)** — [CC] `infra/ai-flip` wiring PR (UserSecretsId, ACA secret + `Ai__*` env via Bicep, deploy.yml passthrough; RUNBOOK §9 is the contract) · [RAZIEL] key + `AI_ENABLED` + live-key smoke per RUNBOOK §9 · then Phase 1 per WBS §10 (FR-AI.2/3 remain Could-priority stubs) · Raziel's post-completion code study of Phase 0 begins now that the gate is closed.
